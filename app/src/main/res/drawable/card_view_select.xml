<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:drawable="@color/md_theme_inversePrimary" android:state_pressed="true"/>
    <!-- Default state -->
    <item android:drawable="@color/md_theme_primaryContainer"/>
</selector>